---
- name: Include install tasks
  include_tasks:
    file: install.yml
  when:
    - "nzbget.enabled | default('false') |bool"

- name: Include uninstall tasks
  include_tasks:
    file: uninstall.yml
  when:
    - "not nzbget.enabled | default('false') |bool"
